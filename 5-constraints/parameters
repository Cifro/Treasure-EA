#  Parameters needed to be set, depending on data

numOfGenerations = 1000

skipGenerations = 50   # How much generations will be skipped

numOfIndividuals = 1

# Do we have a file with best solutions / paths?

hasSolutions = 1     # 0 - no, > 0  yes

